#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    string t;
    cin >> t;
    if (t.find("AC") != string::npos)
        cout << "Yes" << endl;
    else
        cout << "No" << endl;
    return 0;
}