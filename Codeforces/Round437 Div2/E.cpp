#include <iostream>

using namespace std;

const int SIZE = 3e5 + 10;

int stock[SIZE] = {0};

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    long long total = 0;
    cin >> n;
    for (int i = 0; i < n; i++)
        cin >> stock[i];
    for (int i = 0; i < n; i++)
}