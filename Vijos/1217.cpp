#include <iostream>
#include <string>
#include <cstdio>

using namespace std;

int main()
{
    char ch;
    string contest = "";
    int x, y, xcount, ycount;
    while ((ch = cin.get()) && ch != 'E')
        if (ch == 'W' || ch == 'L')
            contest += ch;
    //process 11
    for (int i = 0; i < contest.size(); i++)
    {
    }
    return 0;
}