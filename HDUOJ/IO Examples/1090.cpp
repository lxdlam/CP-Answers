#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int T;
    cin >> T;
    ll a, b;
    while (T--)
    {
        cin >> a >> b;
        cout << a + b << endl;
    }
    return 0;
}